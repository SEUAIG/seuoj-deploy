# 后端子模块结构（services/backend）

## 1. 基本信息

| 项目 | 值 |
|------|---|
| 语言 | Java 21 |
| 框架 | Spring Boot 3.2.0 |
| ORM | MyBatis Plus 3.5.9 |
| 数据库 | MySQL 8.0 |
| 构建工具 | Maven |
| 仓库 | `git@github.com:SEUAIG/seuoj-backend.git` (branch: master) |

## 2. 目录结构

```
services/backend/
├── pom.xml                             # Maven 构建配置
├── Dockerfile                          # 多阶段 Docker 构建
├── sql/
│   ├── database_schema.sql             # 完整数据库 DDL
│   ├── database_init_data.sql          # 初始数据
│   └── docker-init.sql
├── src/main/java/com/seuoj/seuojbackend/
│   ├── SeuojBackendApplication.java    # 入口
│   ├── annotation/                     # 自定义注解
│   │   ├── AllowAnonymous.java         # 允许匿名访问
│   │   └── RequireRole.java            # 角色鉴权
│   ├── aspect/
│   │   └── AuthAspect.java             # AOP 鉴权切面
│   ├── interceptor/
│   │   ├── JwtAuthInterceptor.java     # JWT 认证拦截器
│   │   └── JudgeAuthInterceptor.java   # 判题端认证拦截器
│   ├── config/                         # Spring 配置
│   ├── controller/                     # REST API 控制器
│   ├── service/                        # 业务逻辑层
│   ├── mapper/                         # MyBatis Mapper 接口
│   ├── entity/                         # 数据库实体
│   ├── dto/                            # 请求数据传输对象
│   ├── vo/                             # 响应视图对象
│   ├── model/                          # 领域模型
│   ├── common/                         # 枚举与常量
│   ├── exception/                      # 异常定义与全局处理
│   ├── client/                         # 判题端 HTTP 客户端
│   ├── archive/                        # 压缩包解析
│   ├── access/                         # 题目访问控制
│   ├── storage/                        # 代码存储
│   └── util/                           # 工具类
├── src/main/resources/
│   ├── application.yml.template        # 配置模板
│   └── mapper/                         # MyBatis XML 映射
└── src/test/                           # 集成测试
```

## 3. 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.0 | Web 框架 |
| MyBatis Plus | 3.5.9 | ORM + 分页 |
| Spring Security Crypto | — | BCrypt 密码加密 |
| Spring AOP | — | 声明式鉴权 |
| Spring Mail | — | 邮件验证码 |
| Caffeine | — | 本地缓存 (验证码) |
| Commons Compress | — | 压缩包解析 |
| OkHttp3 MockWebServer | — | 测试用 |

## 4. API 端点清单

### 4.1 认证模块 (`/api/auth`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/login` | 登录 | 公开 |
| POST | `/register` | 注册 | 公开 |
| POST | `/register/send-code` | 发送注册验证码 | 公开 |
| POST | `/reset-password/send-code` | 发送重置密码验证码 | 公开 |
| POST | `/reset-password` | 重置密码 | 公开 |
| POST | `/change-password` | 修改密码 | 登录 |
| POST | `/token/exchange` | Token 交换 | 登录 |

### 4.2 题目模块 (`/api/problem`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/page` | 分页列表 (支持搜索/标签筛选) | 公开 |
| GET | `/{pid}` | 题目详情 (支持竞赛/题单上下文) | 公开 |
| POST | `/` | 创建题目 | ADMIN |
| PATCH | `/edit` | 编辑题目 | ADMIN |
| DELETE | `/{pid}` | 删除题目 | ADMIN |
| GET | `/next_id` | 获取下一个可用题号 | ADMIN |
| POST | `/data/{pid}` | 上传测试数据 (代理到 judgend) | ADMIN |
| GET | `/config/{pid}` | 获取评测配置 | ADMIN |
| PUT | `/config/{pid}` | 更新评测配置 | ADMIN |
| GET | `/tree/{pid}` | 获取题目文件树 | ADMIN |
| GET | `/file/{pid}/{file_name}` | 下载题目文件 | ADMIN |
| DELETE | `/file/{pid}/{file_name}` | 删除题目文件 | ADMIN |

### 4.3 提交模块 (`/api`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/submission` | 提交代码 | 登录 |
| GET | `/submission/{submissionNo}` | 查看提交结果 | 公开 |
| GET | `/submission/page` | 分页列表 (按用户/判定筛选) | 公开 |

### 4.4 题单模块 (`/api/problem_set`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/` | 创建题单 | 登录 |
| GET | `/page` | 分页列表 | 公开 |
| GET | `/{id}` | 题单详情 | 公开 |
| PUT | `/{id}` | 更新题单 | 拥有者 |
| DELETE | `/{id}` | 删除题单 | 拥有者 |
| POST | `/{id}/problem` | 替换题单题目 | 拥有者 |

### 4.5 班级模块 (`/api/class`)

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/` | 创建班级 | TEACHER/ADMIN |
| GET | `/page` | 分页列表 | 公开 |
| PUT | `/{id}` | 更新班级 | TEACHER/ADMIN |
| DELETE | `/{id}` | 删除班级 | TEACHER/ADMIN |
| POST | `/{id}/join` | 加入班级 | 登录 |
| GET | `/{id}/member/page` | 成员列表 | 公开 |
| POST | `/{id}/member/{uid}` | 添加成员 | TEACHER/ADMIN |
| DELETE | `/{id}/member/{uid}` | 移除成员 | TEACHER/ADMIN |
| GET | `/{id}/problem_set/page` | 关联题单 | 公开 |
| PUT | `/{id}/problem_set/{psid}` | 关联题单 | TEACHER/ADMIN |
| DELETE | `/{id}/problem_set/{psid}` | 取消关联 | TEACHER/ADMIN |
| GET | `/{id}/contest/page` | 关联竞赛 | 公开 |
| PUT | `/{id}/contest/{cid}` | 关联竞赛 | TEACHER/ADMIN |
| DELETE | `/{id}/contest/{cid}` | 取消关联 | TEACHER/ADMIN |

### 4.6 竞赛模块（Contest）

竞赛相关接口在 frontend 中被调用，但 backend controller 代码未完整读取。根据前端 API 调用推断：

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/contest/page` | 竞赛列表 |
| GET | `/api/contest/{id}` | 竞赛详情 |
| POST | `/api/contest` | 创建竞赛 |
| PUT | `/api/contest/{id}` | 更新竞赛 |
| GET | `/api/contest/{id}/problems` | 竞赛题目 |
| POST | `/api/contest/{id}/submission` | 竞赛提交 |
| GET | `/api/contest/{id}/submission` | 竞赛提交列表 |
| GET | `/api/contest/{id}/standings` | 排行榜 |
| POST | `/api/contest/{id}/register` | 报名 |
| POST | `/api/contest/{id}/unregister` | 取消报名 |

### 4.7 其他模块

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/api/user/me` | 当前用户信息 | 登录 |
| GET | `/api/me/heatmap` | 提交热力图 | 登录 |
| GET | `/api/problem/tag` | 标签列表 | 公开 |
| POST | `/api/problem/tag` | 创建标签 | ADMIN |
| PUT | `/api/problem/tag` | 更新标签 | ADMIN |
| GET | `/api/common/user/page` | 用户管理列表 | TEACHER/ADMIN |
| POST | `/api/common/user/batch-import` | 批量导入用户 | ADMIN |
| PUT | `/judge/submission/{id}` | 判题结果回调 | judgend |

## 5. Service 层设计

| Service | 职责 | 关键方法 |
|---------|------|---------|
| AuthService | 认证注册 | register, login, exchangeToken, resetPassword, changePassword |
| ProblemService | 题目管理 | getProblemPage, getProblemDetail, createProblem, editProblem, deleteProblem |
| SubmissionService | 提交评测 | submit, getResult, listSubmissions, getHeatmap |
| ProblemSetService | 题单管理 | createProblemSet, getProblemSetDetail, updateProblemSet, replaceProblemSetProblems |
| ClassService | 班级管理 | createClass, joinClass, getClassMemberPage, linkProblemSet, linkContest |
| TagService | 标签管理 | listTags, createTag, updateTag |
| JudgeService | 判题结果 | handleJudgeResult |
| UserRoleService | 角色查询 | getRoleCodesByUserId, isAdmin, hasTeacherRole |
| VerificationCodeService | 验证码 | sendCode, preVerifyCode, consumeVerification |
| CommonService | 通用查询 | getUserPage |
| ProblemAccessService | 访问控制 | 判断用户是否可查看私有题目 |

## 6. 认证与安全

### JWT 实现

- **算法**: Ed25519 (EdDSA 椭圆曲线)
- **Token 类型**: ACCESS (24 小时) / TEMP (5 分钟)
- **Claims**: subject=publicId, token_type, iat, exp
- **密钥管理**: 环境变量注入 Base64 编码的公私钥，未配置时自动生成

### 鉴权流程

1. `JwtAuthInterceptor` 拦截所有请求，解析 JWT → 设置 `UserContextHolder` (ThreadLocal)
2. `AuthAspect` (AOP) 检查 Controller 方法上的 `@AllowAnonymous` / `@RequireRole` 注解
3. 角色不足时抛出 `AuthorizationException`，由 `GlobalExceptionHandler` 转为 HTTP 401/403

### 密码安全

- BCrypt 加密存储，60 字符哈希输出
- 验证码：Caffeine 缓存，300 秒过期，最多 5 次验证尝试，60 秒发送间隔

## 7. 外部集成

### 判题端客户端 (JudgeClient)

```
backend → judgend (HTTP)
├── getProblemData(pid)         GET /judge/problem/{pid}
├── editProblem(request)        PATCH /judge/problem/edit
├── submitForJudging(request)   POST /judge/submission
├── uploadProblemArchive(...)   POST /judge/problem/data/{pid}
├── getProblemConfig(pid)       GET /judge/problem/config/{pid}
└── putProblemConfig(pid, dto)  PUT /judge/problem/config/{pid}
```

认证方式：请求头携带 `JUDGE_SECRET`。

### 邮件服务

- SMTP: smtp.qq.com:465 (SSL)
- 用途：注册验证码、密码重置验证码

## 8. 异常处理体系

| 异常类 | HTTP 状态码 | 错误码 |
|--------|-----------|--------|
| BadRequestException | 400 | 40000 |
| CodeInvalidOrExpiredException | 400 | 40001 |
| AuthorizationException | 401 | 40100/40101 |
| ForbiddenException | 403 | 40300 |
| NotFoundException | 404 | 40400 |
| ConflictException | 409 | 40900 |
| CodeSendTooFrequentException | 429 | 42900 |
| CodeTooManyTriesException | 429 | 42901 |
| InternalServerException | 500 | 50000 |
| JudgeRemoteException | 502 | 50001 |
| CodeStorageException | 500 | 50002 |

所有异常统一由 `GlobalExceptionHandler` 捕获，返回标准 `Result<T>` 结构。

## 9. 配置项

| 配置 | 默认值 | 说明 |
|------|--------|------|
| `jwt.private-key` | — | Ed25519 私钥 (Base64) |
| `jwt.public-key` | — | Ed25519 公钥 (Base64) |
| `jwt.access-expiration` | 86400 | Access Token 有效期 (秒) |
| `jwt.temp-expiration` | 300 | Temp Token 有效期 (秒) |
| `verification.code-expire-seconds` | 300 | 验证码过期时间 |
| `verification.send-interval-seconds` | 60 | 最小发送间隔 |
| `verification.max-verify-fails` | 5 | 最大验证失败次数 |
| `storage.user-code-storage-path` | ./data/user-code | 代码存储路径 |
| `problem.testcase.max-archive-size` | 50 MB | 最大压缩包大小 |
| `problem.testcase.max-entry-count` | 200 | 最大文件数 |
| `problem.testcase.max-entry-size` | 5 MB | 单文件最大 |
| `problem.testcase.max-total-size` | 200 MB | 解压后最大总大小 |
| `judge.server-url` | http://localhost:8081 | 判题端地址 |

## 10. Mapper 清单 (21 个)

| Mapper | 对应表 |
|--------|--------|
| UserInfoMapper | user_info |
| UserRoleMapper | user_role |
| UserRoleRelMapper | user_role_rel |
| ProblemMapper | problem |
| SubmissionMapper | submission |
| ProblemTagRelMapper | problem_tag_rel |
| TagMapper | tag |
| TagGroupMapper | tag_group |
| ProblemSetMapper | problem_set |
| ProblemSetProblemRelMapper | problem_set_problem_rel |
| ProblemSetInvitedMemberRelMapper | problem_set_invited_member_rel |
| UserProblemSetRelMapper | user_problem_set_rel |
| ClassInfoMapper | class_info |
| ClassStudentRelMapper | class_student_rel |
| ClassProblemSetRelMapper | class_problem_set_rel |
| ClassContestRelMapper | class_contest_rel |
| ContestMapper | contest |
| ContestProblemRelMapper | contest_problem_rel |
| ContestRegisterRelMapper | contest_register_rel |
| ContestManagerRelMapper | contest_manager_rel |
| ContestSubmissionMapper | contest_submission |
