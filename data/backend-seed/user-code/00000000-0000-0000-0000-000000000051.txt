#include <iostream>
#include <numeric>
#include <vector>
using namespace std;
// 迷宫寻路
int main() {
    int n; cin >> n;
    vector<int> v(n);
    for (auto& x : v) cin >> x;
    cout << reduce(v.begin(), v.end()) << endl;
}
